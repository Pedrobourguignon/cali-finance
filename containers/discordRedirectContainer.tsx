import { Flex } from '@chakra-ui/react';
import { DiscordRedirectComponent } from 'components';
import React from 'react';

export const DiscordRedirectContainer = () => (
	<Flex>
		<DiscordRedirectComponent />
	</Flex>
);

export default DiscordRedirectContainer;
